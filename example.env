# Optional: UI help chatbot (Page Agent). Without these, Lumen uses built-in heuristics.
PAGE_AGENT_API_KEY=
PAGE_AGENT_BASE_URL=https://api.openai.com/v1
PAGE_AGENT_MODEL=gpt-4o-mini

# Optional: public site URL (used in metadata / share hints)
NEXT_PUBLIC_APP_URL=http://localhost:3000
